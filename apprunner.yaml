version: 1.0
runtime: python311

build:
  commands:
    build:
      - python3 -m pip3 install --target /root/.python_packages/lib/site-packages -r requirements.txt

run:
  command: python3 -m uvicorn main:app --host 0.0.0.0 --port 8000
  network:
    port: 8000
